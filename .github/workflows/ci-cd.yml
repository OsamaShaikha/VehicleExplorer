name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  backend-build-test:
    name: Backend Build & Test
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.0.x'
    
    - name: Restore dependencies
      run: dotnet restore
      working-directory: ./backend
    
    - name: Build
      run: dotnet build --no-restore --configuration Release
      working-directory: ./backend
    
    - name: Test
      run: dotnet test --no-build --configuration Release --verbosity normal
      working-directory: ./backend

  frontend-build-test:
    name: Frontend Build & Test
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: ./frontend/package-lock.json
    
    - name: Install dependencies
      run: npm ci
      working-directory: ./frontend
    
    - name: Lint
      run: npm run lint
      working-directory: ./frontend
    
    - name: Build
      run: npm run build --configuration=production
      working-directory: ./frontend
    
    - name: Test
      run: npm test -- --watch=false --browsers=ChromeHeadless
      working-directory: ./frontend

  docker-build:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: [backend-build-test, frontend-build-test]
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    
    - name: Build Backend Docker Image
      run: docker build -t vehicle-explorer-backend:${{ github.sha }} ./backend
    
    - name: Build Frontend Docker Image
      run: docker build -f ./frontend/Dockerfile.prod -t vehicle-explorer-frontend:${{ github.sha }} ./frontend
    
    - name: Test Docker Compose
      run: docker-compose -f docker-compose.prod.yml config

  # Uncomment and configure for automatic deployment
  # deploy:
  #   name: Deploy to AWS
  #   runs-on: ubuntu-latest
  #   needs: [docker-build]
  #   if: github.ref == 'refs/heads/main'
  #   
  #   steps:
  #   - uses: actions/checkout@v3
  #   
  #   - name: Configure AWS credentials
  #     uses: aws-actions/configure-aws-credentials@v2
  #     with:
  #       aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #       aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #       aws-region: us-east-1
  #   
  #   - name: Deploy to EC2
  #     run: |
  #       # Add your deployment script here
  #       # Example: SSH into EC2 and pull latest changes
  #       echo "Deploying to EC2..."
